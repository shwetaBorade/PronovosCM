<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/selectable_shape"
        android:top="4dp"
        android:bottom="4dp"
        android:left="4dp"
        android:right="4dp">
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent"/>
            <stroke android:color="@android:color/transparent"
                android:width="1dp"/>
        </shape>
    </item>

    <item
        android:id="@+id/circle_shape"
        android:top="8dp"
        android:bottom="8dp"
        android:left="8dp"
        android:right="8dp">
        <shape android:shape="oval">
            <solid android:color="@color/red"/>
            <!-- Size needs to be set here, as pre-23 the layer-list will have
            no reference width/height (annot set width or height pre-23 in drawable).
            -->
            <size android:height="24dp" android:width="24dp"/>
        </shape>
    </item>
</layer-list>
